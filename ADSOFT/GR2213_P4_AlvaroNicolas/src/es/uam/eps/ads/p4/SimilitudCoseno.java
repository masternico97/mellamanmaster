package es.uam.eps.ads.p3;

public class SimilitudCoseno implements Similitud {
    public double sim(Long u, Long v){
        double similitud;
        //sim(u,v)=Sumatorio de i in I(rating.u.i * rating.v.i)/sqrt(Sumatorio de i in I(rating.u.i)²*Sumatorio de i in I(rating.v.i)²)
        
    }
}
